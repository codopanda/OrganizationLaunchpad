# Workers

Place Cloudflare Worker projects here.

Each worker should expose a narrow API surface and delegate actual business behavior to reusable application code rather than embedding logic directly in request handlers.

Suggested convention:

```text
workers/<worker-name>/
```

